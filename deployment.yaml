apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-api
  labels:
    app: sample-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-api
  template:
    metadata:
      labels:
        app: sample-api
    spec:
      containers:
        - name: sample-api
          image: swarajwohlig2107/171352791656.dkr.ecr.ap-south-1.amazonaws.com/my-ecr:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: sample-config
